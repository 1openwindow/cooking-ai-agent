# Not CEA python template for Teams Bot

Developer usually use AITK to generate a `python` AI agent with their own AI endpoint and API key. However, ATK CEA template only supports `typescript` and `javascript`.

